apiVersion: apps/v1
kind: Deployment
metadata:
  name: shelter-service
  namespace: shelter-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shelter-service
  template:
    metadata:
      labels:
        app: shelter-service
    spec:
      containers:
      - name: shelter-service
        image: ghcr.io/algoldenberg/shelter-route-planner/shelter-service:latest
        ports:
        - containerPort: 8001
        env:
        - name: MONGODB_URL
          value: "mongodb://admin:shelterpass123@mongodb:27017"
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"